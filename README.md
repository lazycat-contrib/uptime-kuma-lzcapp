# Uptime Kuma for LazyCat

LazyCat LPK v2 conversion for `louislam/uptime-kuma:2.5.0`.

The runtime image is configured through the requested Docker accelerator:

```text
docker.1ms.run/louislam/uptime-kuma:2.5.0
```

Persistent application data is stored under `/lzcapp/var/data`.
